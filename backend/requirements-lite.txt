# Lightweight API deps without OCR (for size-constrained hosts).
# Scanned/image-only PDFs will fail extraction until OCR deps are installed
# via: pip install -r requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
bcrypt>=4.2.0
pdfplumber>=0.11.4
PyMuPDF>=1.24.0
openpyxl>=3.1.5
