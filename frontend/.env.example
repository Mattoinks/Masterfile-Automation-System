# Backend API base URL.
# Local (Vite proxy): /api
# Local (direct / Docker): http://localhost:8000/api
# Production (Vercel → Render/Railway): https://YOUR-BACKEND.onrender.com/api
VITE_API_BASE=/api
